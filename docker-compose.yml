services:
  app:
    build: .
    ports:
      - "${HOST_PORT:-3000}:3000"
    volumes:
      - ${MUSIC_PATH:-./music}:/music:ro
    environment:
      - MUSIC_DIR=/music
      - SITE_TITLE=${SITE_TITLE:-justfplay}
    restart: unless-stopped
